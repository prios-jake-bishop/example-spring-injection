# example-spring-injection

See BeanInjectionTest and ComponentInjectionTest, both exercise Spring Injection of configured beans and components, respectively.

To exercise all tests from the command line:
```
./gradlew test
```
